# Phase 4 — Handlers

## Status: SKIPPED

### Reason

senno exposes no HTTP endpoints. The agent engine produces an event stream the
host application's handlers consume; the only HTTP-framework touchpoint is the
axum SSE adapter covered in Phase 9 (integration/features).

### Notes

- This phase was evaluated and intentionally skipped.
